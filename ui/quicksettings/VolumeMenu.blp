using Gtk 4.0;

template $VolumeMenu: $PanelRow {

    Scale slider {
        hexpand: true;
        valign: center;
        draw-value: false;

        adjustment: Adjustment {
            lower: 0;
            upper: 1.0;
            value: bind template.value bidirectional;
        };
    }
}
