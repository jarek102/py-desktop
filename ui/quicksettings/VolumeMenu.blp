using Gtk 4.0;

template $VolumeMenu: Box {
    orientation: vertical;

    $PanelRow panel_row {
        icon-name: bind template.icon-name;
        toggle-visible: false;
        caption: bind template.active-device-label;
        caption-visible: true;
    }
}
