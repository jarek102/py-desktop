using Gtk 4.0;
using Astal 4.0;

template $Overlay : Astal.Window {
  anchor: top | bottom | left | right;
  layer: top;
  exclusivity: ignore;
  keymode: none;
  css-classes: ["overlay"];

  Gtk.GestureClick {
    pressed => $on_pressed();
  }
}
